meta {
  name: criarPrato
  type: http
  seq: 2
}

post {
  url: {{backend_url}}:{{backend_port}}/nutria/prato/criar
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenBearer}}
}

body:json {
  {
    "nome_prato": "03",
    "dtt_criacao_prato": "CURRENT_TIMESTAMP",
    "prato_favoritado": false
  }
}

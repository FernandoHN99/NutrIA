meta {
  name: atualizarPrato
  type: http
  seq: 3
}

patch {
  url: {{backend_url}}:{{backend_port}}/nutria/prato/atualizar
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenBearer}}
}

body:json {
  {
    "nome_prato": "03",
    "prato_favoritado": true
  }
}
